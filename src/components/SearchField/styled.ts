import { Paper as PaperStyled } from '@material-ui/core';
import styled from 'styled-components';

export const Paper = styled(PaperStyled)`
  border: 1px solid;
  border-color: rgba(255, 255, 255, 0.23);
`;
