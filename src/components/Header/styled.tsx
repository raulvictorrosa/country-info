import Typography from '@material-ui/core/Typography';
import styled from 'styled-components';

export const ThemeSwitcherTitle = styled(({ ...props }) => (
  <Typography {...props} />
))`
  padding: 0 5px;
`;
